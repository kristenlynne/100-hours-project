<%- include('partials/loggedinheader') -%>
  <div class="container">

    <div class="">
      <ul class="">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="">
            <a href="/post/<%= posts[i]._id%>">
              <img class="" src="<%= posts[i].image%>">
            </a>
          </li>
          <div>
            <button class="fa fa-heart" type="submit"> <%= posts[i].likes %></button>
            <a href="#comment"><i class="fa-regular fa-comment"></i></a>
            <a href="#"><i class="fa-regular fa-bookmark"></i></a> <!-- add bookmark functionality -->
          </div>
          <div class="mt-5">
            <p><b><a href="#"><%= userName[i] %></a></b> <%= posts[i].caption %></p>
          </div>
        <% } %>
        </ul>
    </div>

  </div>  
<%- include('partials/footer') -%>